//! isopod-guest-agent — PID 1 inside the guest.
//!
//! Scaffold placeholder; implementation lands in M2. Build target:
//! x86_64-unknown-linux-musl (static).

fn main() {
    println!("isopod-guest-agent placeholder");
}
