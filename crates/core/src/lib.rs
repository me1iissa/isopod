//! isopod-core — orchestrator library.
//!
//! Module layout per PLAN.md: vm, stage, snapshot, net, agent, store, image.
//! Modules land milestone by milestone; M1 ships `paths` and `image`.

pub mod image;
pub mod paths;
